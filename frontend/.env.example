# Pinata JWT (recommended)
# 1) Create a JWT in Pinata dashboard
# 2) Copy it here
VITE_PINATA_JWT=YOUR_PINATA_JWT

# Optional: override gateway (defaults to https://gateway.pinata.cloud/ipfs)
# VITE_IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs
